package cursodevdojo.introducao;

public class CursoJava {
    public static void main(String[] args) {
        /*
        Comandos de entrada
        next();
        nextInt();
        nextDouble();
        next().charAt(0)

        Locale.setDefault(Locale.US);
        printf(%.2f%n) so escreve dois números após a virgula
        Exemplo: double a = 2,231
        System.out.printf("O valor de a é:%.2f%n" , a)
        Operações matemáticas
        Math.sqrt(x) Raiz quadrada de x
        Math.pow(x , y); X elevado a Y
        Math.abs(x) Valor absoluto de x

        double a = 2.231d;
        System.out.printf("O valor de a é:%.2f%n" , a);

        a+=b a+b=a
        a*=b a*b=a

        Switch case
        switch(x){
            case 1:
                dia="domingo";
                break;
            case 2:

            default:
                dia="Valor inválido";
         }

         Expressão condicional ternária - Alternativa ao if else
         (condição) ? valor_se_verdadeiro : valor_se_falso

         double preço= 34;
         double desconto = (preço<20) ? preço*0,1: preço*0,05;

         Classe:
         - Tipo estruturado que pode conter membros (Atributos(dados/campos)) (Métodos(funções/operações))
         Pode prover construtores, sobrecarga, encapsulamento, herança, polimorfismo
         Entidade, serviço, controladores, utilitários

        Instanciação

        Toda classe Java é uma sub classe da classe Object

         */
    }
}
